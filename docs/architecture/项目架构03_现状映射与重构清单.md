# 项目架构03 现状映射与重构清单

更新时间：2026-03-23

## 本轮增量（发布-线索-客户链路补齐）

### 1. 迁移脚本补齐

- 已新增 Alembic 迁移脚本：`backend/alembic/versions/20260323_01_add_leads_and_customer_lead_id.py`
- 覆盖内容：
	- 新增 `leads` 表
	- `customers` 新增 `lead_id` 外键（唯一）
- 注记：当前仓库未见完整 Alembic 运行骨架（如 `alembic.ini`、`alembic/env.py`），迁移版本文件已补齐，执行链路需在部署侧补全。

### 1.1 迁移骨架补全（本轮继续完成）

- 已新增：`backend/alembic.ini`
- 已新增：`backend/alembic/env.py`
- 已新增：`backend/alembic/script.py.mako`
- 已具备可执行命令：
	- `alembic current`
	- `alembic upgrade head`
	- `alembic downgrade -1`
	- `alembic history --verbose`

### 1.2 迁移兼容处理与结果

- 历史库存在旧 revision：`20260323_0008`，仓库中原先缺失该文件。
- 已新增兼容桥接迁移：`backend/alembic/versions/20260323_0008_legacy_baseline.py`。
- 本轮迁移链路：`20260323_0008 -> 20260323_01`。
- 已在本地库完成实跑升级，当前版本为：`20260323_01 (head)`。

### 2. 线索人工转客户

- 新增接口：`POST /api/lead/{lead_id}/convert-customer`
- 桌面端线索页新增“转客户”按钮，触发人工确认流转。

### 3. 发布链路跳转

- 发布任务列表新增“查看链路”操作
- 通过 `GET /api/publish/tasks/{task_id}/trace` 跳转至线索页并定位目标线索
- 若已转客户，支持带参数跳转并在客户页高亮目标客户

### 本轮产出价值

- 主链路从“发布回填 -> 自动线索”扩展为“发布回填 -> 线索池运营 -> 人工确认转客户”
- 运营端具备可视化追踪能力：`publish_task -> lead -> customer`

### 下一步建议

- 增加线索分配记录表与操作日志，补齐“谁在何时做了什么”
- 在客户页增加来源线索展示字段，进一步强化追溯
- 将 trace 能力扩展到客户详情页

### 本轮校验结论

- 代码层：本轮改动文件静态检查通过。
- 测试层：新增 lead 人工转客户与 trace 用例断言，确保 `publish_task -> lead -> customer` 链路可回查。

### 前端验收收口

- 已补“可逐项打勾”验收清单：`docs/operations/验收清单_发布线索客户闭环_2026-03-23.md`。
- 验收单覆盖主链路：`发布任务查看链路 -> 线索转客户 -> 客户高亮`。
- 验收单覆盖异常项：无链路提示、重复转客户保护、权限校验。

### 全功能落地跟进（2-3周）

- 已新增执行路线图：`docs/operations/全功能落地计划_2026-03-23.md`
- 已新增功能看板：`docs/operations/功能跟进看板_全功能落地_2026-03-23.md`
- 后续每次增量开发需同步更新“看板勾选 + 证据链接 + 问题池状态”。

### Week1 进度（已完成）

- 登录态守卫统一：未登录拦截、登录后回跳、401 自动清理并回到登录。
- 素材编辑器增强：草稿自动保存/恢复、平台长度约束、提交回流。
- 仪表板增强：异常态提示 + 业务快捷入口。
- insight 批量分析：已接入限流器并返回限流窗口参数。

### Week2-3 进度（已完成）

- Electron 运行时 API 基地址接入：支持基于后端端口动态切换。
- 浏览器插件联调文档补齐：新增双浏览器联调与回归清单。
- 数据导出：新增客户与发布任务 CSV 导出接口与前端导出按钮。
- 企业微信：新增测试通知接口（webhook 模式）。
- RBAC 最小版：新增用户角色（admin/operator/viewer）及导出权限控制。

## 目标

把 docs/项目架构03 的蓝图映射到当前仓库，明确：

- 已落地模块
- 部分落地模块
- 当前缺口
- 推荐实施顺序

## 已落地

### 后端

- 鉴权：已有注册、登录、当前用户接口
- 素材中心：已有 content 旧接口，已有 v1 collect 采集接口
- 爆款洞察：已有主题、导入、分析、检索、统计接口
- AI 工作台：已有多平台改写、图片理解
- 合规审核：已有规则检查接口
- 客户管理：已有客户列表、跟进、待跟进接口
- 发布记录：已有发布记录创建、更新、统计来源数据
- 数据看板：已有概览、趋势、平台、主题、高质量内容、AI 调用统计

### 前端

- 桌面端已有登录、看板、素材中心、爆款洞察、AI 工作台、合规、客户、发布页面
- 新增采集中心页面，作为多入口采集的桌面工作台入口
- 新增收件箱页面，支持分析、入库、丢弃分拣动作

### 已打通的新闭环

- 浏览器插件采集 -> plugin_collections
- 浏览器插件采集 -> content_assets
- 浏览器插件采集 -> insight_content_items

这意味着当前项目已经具备：

采集入库 -> 洞察沉淀 -> AI 改写参考

## 部分落地

### 采集接入层

- 已有：桌面端采集中心、链接解析、手工录入、浏览器插件接口、统一采集入口 `/api/v1/collect/intake`
- 现状：mobile-h5 最小可运行页面已补齐（share-collect / screenshot-ocr / quick-note / wecom-redirect）
- 仍缺：企业微信 OAuth 鉴权、OCR 失败重试与弱网兜底、移动端 UAT 证据沉淀

### 素材收件箱

- 已有：独立 inbox 表、收件箱列表、状态流转、分析、入素材库、丢弃动作、批量指派/丢弃/入库、去重预览
- 缺少：真正的分拣责任人字段（当前为 review_note 轻量指派）、去重自动合并策略、审核备注规范前端约束

### 素材中心

- 已有：素材基础表、标签、分类、爆款评分、搜索筛选基础能力
- 缺少：片段库、反例库、选题池、附件表、评分细表

### 发布任务

- 已有：发布记录 + publish task 最小状态机（创建、分配、领取、回填、驳回、关闭、trace）
- 缺少：跨角色审批流、SLA 超时升级策略、任务看板可视化

### CRM

- 已有：客户主表、跟进记录 JSON、状态筛选
- 缺少：线索池、客户扩展资料、阶段日志、文件资料、线索分配记录

## 当前缺口

### 业务域缺口

- 收件箱域
- 账号台账域
- 线索域
- 企业微信协同域
- 知识库域
- 审计域

### 终端缺口

- mobile-h5 已落最小页面并可直连 v1 collect/ocr，仍需做鉴权收口与移动端 UAT
- browser-extension 已补齐 MV3 最小可运行实现（manifest/content/background/popup），待双浏览器 UAT 执行记录持续沉淀

### 基础设施缺口

- Redis、MinIO、pgvector 仍主要停留在部署预留层
- 异步任务与通知任务目前仍是占位实现

## 风险状态更新（2026-03-23）

### 风险一：迁移治理

- 状态：部分收敛
- 说明：Alembic 骨架和迁移链已补齐并实跑，但仍需在生产发布流程中“强制迁移优先、禁用 create_all 改表”。

### 风险二：批量 AI 分析限流

- 状态：部分收敛
- 说明：`/api/insight/analyze/batch` 已接入 `DistributedRateLimiter` 并回传限流窗口；仍缺任务日志查询与可观测面板。
- 下一步：补任务维度查询接口与前端状态页。

### 风险三：插件采集与洞察打通

- 状态：主链已打通，回归执行中
- 说明：`/api/v1/ai/plugin/collect` 已同步写入 `content_assets` 与 `insight_content_items`；插件端已补齐最小可运行实现，需持续补充双浏览器回归执行记录与去重一致性证据。

## 推荐实施顺序

### P0：主链路补齐

1. inbox 模块：把 collect 从“直接入素材”升级为“先入收件箱，再确认入素材”
2. publish 从 record 升级为 task + feedback 双表结构
3. lead 模块：把发布结果与客户沉淀中间层补上

### P1：运营效率补齐

1. social account 台账
2. mobile-h5 分享采集页 / 截图上传页 / quick-note 页
	- 进度：已完成最小可运行版，下一步聚焦 OAuth、弱网与重试策略
3. wecom 机器人配置与提醒推送

### P2：知识与智能体补齐

1. knowledge base / document / chunk
2. agent config / task
3. 检索增强、prompt 版本管理、审计留痕

## 本次改动对蓝图的直接贡献

- 修正桌面端采集与 AI 调用到 v1 路由，避免前后端路径错位
- 新增桌面端采集中心页面，补齐蓝图中的采集接入工作台入口
- 打通浏览器插件采集自动写入素材库与洞察库，减少重复录入
- 新增 inbox 最小闭环，采集中心已切换为先入收件箱、再分拣入素材

## 后续实施约束

- 新功能优先落到 docs/项目架构03 对应领域目录，不再继续平铺到旧结构
- 优先新增兼容层，不直接破坏现有 content/customer/publish 旧接口
- 每补一个领域，同时补最小可回归测试

## 全项目跑通还需补齐（2026-03-23）

以下清单用于回答“从当前状态到端到端稳定跑通，还缺哪些关键项”。

### A. 文档与版本口径先统一（P0）

- 迁移版本口径需统一为当前仓库状态：
	- `backend/alembic/versions` 已包含 `20260323_03_add_inbox_assignment_fields.py`
	- 本文与部分周报仍停留在 `20260323_01/02`，需同步修订，避免发布判断失真。
- 部署主路径需统一：
	- 当前可执行主路径仍是 `backend/docker-compose*.yml`
	- `deploy/docker-compose.yml` 仅 `extends backend`，尚未形成完整独立编排。
- 运行手册需补“单一真相源”：
	- 明确开发、预发、生产三套命令与验收口径，避免 README、部署文档、周计划描述不一致。

### B. 端到端链路补齐（P0）

- 采集接入多端补齐：
	- `mobile-h5` 已具备可提交页面并接入后端，完成“可运行”阶段。
	- 截图 OCR 已接入上传识别入口，缺弱网重试与压缩策略。
	- 微信转发入口已补前端承接页，缺 OAuth 鉴权收口。
- 发布任务域补齐：
	- task + feedback 状态机后端已具备最小闭环，下一步需补跨角色审批与前端全量联动。
- 线索运营补齐：
	- 已有线索转客户最小链路，但缺线索分配记录、操作日志、责任人视图与审计字段。

### C. 可观测与可运维补齐（P0）

- insight 批量分析已有限流，但缺“任务日志查询 API + 前端状态页 + 失败重试可视化”。
- 发布/采集/转化链路需统一追踪 ID（trace_id/request_id）贯穿日志，支持故障定位。
- 需补最小监控面：
	- 健康检查、错误率、任务积压、队列耗时、外部依赖可用性（Redis/Ollama/Ark）。

### D. 部署与基础设施补齐（P0）

- 生产迁移治理需制度化：
	- 发布前强制 `alembic upgrade head`，并校验 current=head。
	- 禁止通过 `create_all` 隐式改表。
- `deploy/` 目录需补齐目标编排（而非仅 extends）：
	- Postgres、Redis、Backend、Nginx、（可选）MinIO、监控组件的环境分层配置。
- 补备份与恢复演练：
	- 数据库备份、恢复、回滚演练记录与RTO/RPO目标。

### E. 安全与权限补齐（P1）

- RBAC 现为最小版，需补齐：
	- 细粒度资源权限（导出、批量操作、审计查询）
	- 关键操作审计（谁、何时、改了什么）
- 敏感配置治理：
	- SECRET_KEY、数据库口令、ARK API Key 统一走环境注入与最小权限。

### F. 测试与验收补齐（P0）

- 自动化测试分层目标：
	- 单元：领域服务与权限边界
	- 集成：collect -> inbox -> insight -> publish -> lead -> customer
	- 回归：插件采集（Chrome/Edge）+ 桌面端关键路径
- 必补失败场景：
	- 401/403、429、外部模型超时、数据库连接闪断、重复采集冲突。

## 建议修改动作（文档层）

1. 统一迁移版本描述为“当前 head 以仓库实际版本为准（截至本文为 20260323_03）”。
2. 在部署文档增加“发布前检查闸门”：迁移校验、健康检查、关键链路抽样、回滚演练记录。
3. 在功能看板新增“全项目跑通闸门”泳道（必须全部勾选才能标记可上线）。
4. 在周报模板新增“口径一致性检查”项，防止 README/架构文档/看板三处信息漂移。
5. 增加执行清单：`docs/operations/P0冲刺清单_2026-03-23_后续.md`，用于跨角色并行推进。

## 跑通验收闸门（建议执行顺序）

1. 先过基础设施闸门：数据库、Redis、对象存储（如启用）、模型服务可用。
2. 再过迁移闸门：`alembic current` 与 `head` 一致。
3. 再过后端闸门：核心 API 冒烟 + 关键权限校验通过。
4. 再过前端与桌面端闸门：登录、编辑、发布、线索、客户全链路可操作。
5. 再过插件闸门：Chrome/Edge 均可采集并稳定回传。
6. 最后过运维闸门：监控告警、回滚演练、问题记录模板可执行。