# UAT执行记录（发布-线索-客户闭环）

执行日期：2026-03-23
执行人：Copilot 自动化回归 + 待人工补充
对应验收单：docs/operations/验收清单_发布线索客户闭环_2026-03-23.md

## 1. 自动化执行范围

- 后端聚焦用例：test_main.py::TestLeadPool::test_lead_status_and_assign
- 执行命令：

```bash
cd backend
python -m pytest -q test_main.py -k "lead_status_and_assign"
```

- 执行结果：通过（1 passed, 11 deselected）

### 本轮补充（采集接入 + 收件箱 + 权限流）

- 后端聚焦用例：
	- `test_v1_plugin_collect_syncs_to_content_and_insight`
	- `test_v1_collect_intake_to_inbox_with_dedupe_hint`
	- `test_v1_collect_intake_requires_auth`
	- `test_v1_collect_intake_invalid_source_type`
	- `test_v1_inbox_batch_actions_and_dedupe_preview`
	- `test_lead_convert_customer_forbidden_for_non_owner`
- 执行命令：

```bash
cd backend
python -m pytest test_main.py -k "collect_intake or inbox_batch_actions_and_dedupe_preview or lead_convert_customer_forbidden_for_non_owner or v1_plugin_collect_syncs_to_content_and_insight" --disable-warnings --junitxml=pytest-focus.xml
```

- 执行结果：通过（6 passed, 0 failed, 0 skipped）

## 2. 已自动验证项（可判定）

- [x] 发布任务回填后可生成线索（publish_task -> lead）
- [x] 线索状态更新接口可用
- [x] 线索归属接口可用
- [x] 线索转客户接口可用
- [x] lead trace 可回查 customer_id
- [x] lead/list 返回 customer_id（本轮补齐）

## 3. 待人工页面验收项（需前端点击）

- [ ] 发布任务页点击“查看链路”后，线索页高亮目标线索
- [ ] 线索页点击“转客户”后，出现“已转客户”禁用态
- [ ] 线索页“查看客户”可跳转并在客户页高亮
- [ ] 线索页顶部“打开客户”快捷按钮行为正确
- [ ] 客户页顶部来源提示文案正确

## 4. 环境问题修复记录

- 问题：passlib + bcrypt 在当前环境触发哈希异常，导致测试注册登录阶段失败。
- 处理：将默认哈希方案调整为 pbkdf2_sha256，保留 bcrypt 验证兼容。
- 影响：新注册用户使用 pbkdf2_sha256；历史 bcrypt 哈希仍可走兼容验证路径。
- 变更文件：backend/app/core/security.py

## 5. 本次UAT结论

- 自动化结论：通过
- 人工页面结论：待执行
- 综合结论：有条件通过（可进入人工UI验收）

## 6. 人工补录区

- 验收人：
- 验收时间：
- 环境：
- 最终结论：通过 / 有条件通过 / 不通过
- 问题备注：
