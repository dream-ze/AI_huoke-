global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'default'

receivers:
  - name: 'default'
    # 配置告警接收方式（企业微信/邮件等）
    # webhook_configs:
    #   - url: 'http://your-webhook-url'
