# 采集模块验证存档（2026-03-26）

## 1. 验证目标
- 验证本轮“采集字段语义收敛 + 标准化层”改动可通过基础质量检查。
- 按最短路径补齐三类测试并执行通过。
- 启动服务并验证 Excel 导出链路可用。

## 2. 验证环境
- OS: Windows
- 项目目录: `d:/Playwright爬虫/browser_collector`
- Python 环境: Conda(base), Python 3.13.11

## 3. 执行记录
### 3.1 pytest（三类测试）
命令:
```powershell
pytest -q tests
```
结果:
- `8 passed in 0.26s`
- 退出码 `0`

覆盖说明:
- normalizer 纯函数单元测试（文本清洗、URL 规范化、图片过滤、质量与互动评分）
- parser/collector 字段映射测试（固定 HTML 夹具）
- API 契约测试（`/api/collect/run`、`/api/collect/detail`，含请求校验）

### 3.2 语法编译校验
命令:
```powershell
python -m compileall app
```
结果:
- 编译通过，覆盖 `app/collectors`、`app/normalizers`、`app/schemas`、`app/utils` 等目录

### 3.3 静态错误检查
范围:
- `app/`

结果:
- `No errors found`

### 3.4 启动程序与 Excel 验证
启动命令:
```powershell
uvicorn app.main:app --host 127.0.0.1 --port 8005
```

健康检查结果:
- `{'success': True, 'message': 'ok', 'browser_ready': True, 'login_state_ready': True, 'storage_state_exists': True}`

导出验证:
- 调用 `/api/collect/run` 参数：`keyword=贷款`、`max_items=3`、`need_detail=false`
- 接口返回条数：`3`
- 已生成文件：`exports/collect_贷款_20260326_152038.xlsx`

## 4. 本轮变更文件
- `app/collectors/xiaohongshu_collector.py`
- `app/schemas/result.py`
- `app/utils/excel_export.py`
- `app/normalizers/content_normalizer.py`
- `tests/test_content_normalizer.py`
- `tests/test_xiaohongshu_collector_mapping.py`
- `tests/test_api_contract.py`
- `tests/fixtures/xiaohongshu_list_card.html`
- `验证存档_20260326.md`

## 5. 结论
- 当前代码通过基础验证（测试+语法+静态检查）。
- 三类测试已补齐并通过。
- 本地服务可启动，Excel 导出链路验证通过。

## 6. 后续建议（最短路径）
- 将测试纳入 CI（如 GitHub Actions），每次推送自动执行 `pytest -q tests`。
- 增加带真实样本的 Excel 结构断言，防止字段名变更导致导出空列。
